name: Build ZMK firmware

on:
  push:
    paths-ignore:
      - 'keymap-drawer*'
      - 'draw-keymaps.yml'
      - 'svg_to_png.yml'
  pull_request:
    paths-ignore:
      - 'keymap-drawer*'
      - 'draw-keymaps.yml'
      - 'svg_to_png.yml'
  workflow_dispatch:

jobs:
  build:
    uses: zmkfirmware/zmk/.github/workflows/build-user-config.yml@main
